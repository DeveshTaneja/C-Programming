#include <stdio.h>

void main()
{
    int i;
    char str[50];
    scanf("%s", str);
    
    for(i=0;str[i]!='\0';i++){
        if (str[i]>='a' && str[i]<='z'){
            str[i]=str[i]-('a'-'A');//changing ASCII values from lower to upper
        }
        
    }
    printf("%s",str);

}
